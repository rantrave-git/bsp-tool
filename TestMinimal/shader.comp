#version 450

layout (binding = 0) uniform ParameterUBO {
    float deltaTime;
} ubo;

layout(std140, binding = 1) readonly buffer PointsBuffer {
   vec4 pos[ ];
};

layout(std140, binding = 2) buffer AnglesBuffer {
   float angle[ ];
};

layout (local_size_x = 256, local_size_y = 1, local_size_z = 1) in;

void main() 
{
    uint index = gl_GlobalInvocationID.x;  
}